{% extends 'base.html.twig' %}

{% block title %}Disponibilidad index{% endblock %}

{% block body %}
    <h1>Disponibilidad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Disponibilidad</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for disponibilidad in disponibilidads %}
            <tr>
                <td>{{ disponibilidad.id }}</td>
                <td>{{ disponibilidad.disponibilidad }}</td>
                <td>
                    <a href="{{ path('disponibilidad_show', {'id': disponibilidad.id}) }}">show</a>
                    <a href="{{ path('disponibilidad_edit', {'id': disponibilidad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('disponibilidad_new') }}">Create new</a>
{% endblock %}
