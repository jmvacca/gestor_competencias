<form method="post" action="{{ path('competencia_deportiva_delete', {'id': competencia_deportiva.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competencia_deportiva.id) }}">
    <button class="btn">Delete</button>
</form>
